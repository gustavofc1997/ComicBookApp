<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools2="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/custom_button_padding_horizontal">
    <ImageView
        android:id="@+id/ivComic"
        android:layout_width="120dp"
        android:scaleType="centerInside"
        android:layout_height="180dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools2:ignore="ContentDescription" />

    <LinearLayout
        android:layout_marginStart="@dimen/margin_left"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/ivComic"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvComicTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvComicDate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/custom_button_margin_bottom" />
    </LinearLayout>

</RelativeLayout>